#include "main.h"
/**
 * add - adds two integers and return theresult
 * @a: the first num
 * @b: the second num
 *
 * Return: the addition of the numbers
 */
int add(int a, int b)
{
	return (a + b);
}
